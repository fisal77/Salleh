<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_height="match_parent"
    android:layout_width="match_parent">
    <solid android:color="@color/sallemBlack"/>
    <stroke
        android:width="2dp"
        android:color="@color/borderColor"
        android:height="10dp"
        />
    <corners
        android:radius="5dp"
        />
</shape>

